defaults:
  - model/pi0_5@actor.model
  - training_backend/fsdp@actor.fsdp_config
  - override hydra/job_logging: stdout

hydra:
  run:
    dir: .
  output_subdir: null
  searchpath:
    - file://${oc.env:EMBODIED_PATH}/config/

cluster:
  num_nodes: 1
  component_placement:
    actor,env,rollout: 0-0

runner:
  task_type: sft
  logger:
    log_path: "../results"
    project_name: rlinf
    experiment_name: "real_world_joint_sft_openpi_pi05"
    logger_backends: ["tensorboard"]
  max_epochs: -1
  max_steps: 100000
  val_check_interval: -1
  save_interval: 2000
  log_interval: 100

data:
  dataset_type: openpi_real_world_joint
  train_data_paths: ${oc.env:REAL_WORLD_DATA_ROOT,/inspire/qb-ilm2/project/robot-body/public/real_world_data_dreamzero_motion}
  real_world_joint_root: ${data.train_data_paths}
  lazy_load: true
  num_workers: 2
  prefetch_factor: 1
  pin_memory: true
  parquet_cache_size: 16
  video_backend: torchcodec
  video_tolerance_s: 0.1
  real_world_joint_fast_local_index: true
  real_world_joint_max_tasks: null
  real_world_joint_max_tasks_per_tag: null
  real_world_joint_tags: null
  real_world_joint_task_name_contains: null
  openpi_real_world_joint_sampling_mode: fixed_window
  openpi_real_world_joint_max_chunk_size: 1
  openpi_real_world_joint_num_frames: 1
  openpi_real_world_joint_max_state_dim: 32
  openpi_real_world_joint_use_state: false
  default_instruction: "perform the default behavior."

actor:
  group_name: "ActorGroup"
  training_backend: "fsdp"
  micro_batch_size: 8
  global_batch_size: 128
  seed: 42

  model:
    precision: null
    model_path: "/inspire/hdd/project/robot-body/linbokai-CZXS24250037/RLinf/checkpoints/pi05_base_pytorch_real_world_joint"
    num_action_chunks: 48
    action_dim: 32
    add_value_head: False
    openpi:
      config_name: "pi05_real_world_joint"
      detach_critic_input: True
      num_images_in_input: 3
      train_expert_only: True
      action_chunk: ${actor.model.num_action_chunks}
      action_env_dim: ${actor.model.action_dim}

  optim:
    lr: 2.5e-5
    value_lr: 1.55e-4
    adam_beta1: 0.9
    adam_beta2: 0.95
    adam_eps: 1.0e-08
    weight_decay: 1.0e-10
    clip_grad: 1.0
    warmup_style: "cosine"
    lr_warmup_steps: 1000
    total_training_steps: 100000

  fsdp_config:
    strategy: "fsdp"
    sharding_strategy: "full_shard"
    use_orig_params: False
    gradient_checkpointing: False
    mixed_precision:
      param_dtype: ${actor.model.precision}
      reduce_dtype: ${actor.model.precision}
      buffer_dtype: ${actor.model.precision}
